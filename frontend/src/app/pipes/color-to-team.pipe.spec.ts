import { ColorToTeamPipe } from './color-to-team.pipe';

describe('ColorToTeamPipe', () => {
  it('create an instance', () => {
    const pipe = new ColorToTeamPipe();
    expect(pipe).toBeTruthy();
  });
});
